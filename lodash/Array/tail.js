/*
 * @Author: yangxuefeng yangxuefeng@clinbrain.com
 * @Description: 获取 的除第一个元素之外的所有元素array。
 */
{
  _.tail([1, 2, 3]);
  // => [2, 3]
}

/**
 * 注意区分
 * head: 获取数组的第一个元素
 * tail: 获取数组的除第一个元素之外的所有元素
 * initial: 获取数组的除最后一个元素之外的所有元素
 * last: 获取数组的最后一个元素
 *  */
